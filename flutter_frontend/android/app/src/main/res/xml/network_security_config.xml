<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for LeafCompass.
  Allows cleartext HTTP traffic to the local development backend (10.0.2.2 = Android emulator localhost).
  In production, replace with HTTPS and remove cleartext permission.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
